# Проект "Виджет банковских операций клиента"
## Описание:
Это виджет, который показывает несколько последних успешных банковских операций клиента.
## Установка:
* Клонируйте репозиторий:
  ```
  https://github.com/kveremyeva/homework_python.git
  ```
* Установите `poetry`
  ```
  pip install poetry
  ```
  Если `poetry` установлен, установите зависимости:
  ```
  # для первичной установки
  poetry install
  # для обновления
  poetry update
  ```
  Для просмотра установленных зависимостей используйте `poetry show --tree`
## Как работать
На данный момент реализованы различные модули, многие из них уже протестированы.

## Модуль `main`
Функция `main` отвечает за основную логику проекта и связывает функциональности между собой. Ожидаемое поведение программы должно быть следующим:

Программа приветствует пользователя:
>_Привет! Добро пожаловать в программу работы_ 
_с банковскими транзакциями._ 
_Выберите необходимый пункт меню:_
>1. _Получить информацию о транзакциях из JSON-файла_
>2. _Получить информацию о транзакциях из CSV-файла_
>3. _Получить информацию о транзакциях из XLSX-файла_

Пользователь выбирает необходимую цифру (1, 2, 3), например:

> 1

Программа информурует о том, какой файл был выбран:
> _Для обработки выбран JSON-файл._

После пользователь выбирает статус интересующих его операций:

>_Введите статус, по которому необходимо выполнить фильтрацию. 
Доступные для фильтровки статусы: EXECUTED, CANCELED, PENDING_

Пользователь выбирает необходимый статус:
>  EXECUTED

Программа информирует, что операции отфильтрованы:
> _Операции отфильтрованы по статусу "EXECUTED"_

Если клиент ввел несуществующий/неверный статус, программа сообщает об этом:

> _> test_
> 
> _Программа: Статус операции "test" недоступен._
> 
> _Введите статус, по которому необходимо выполнить фильтрацию. 
Доступные для фильтровки статусы: EXECUTED, CANCELED, PENDING_

После фильтрации программа выводит следующие вопросы для уточнения выборки операций, необходимых пользователю, и выводит в консоль операции, соответствующие выборке пользователя:

>_Отсортировать операции по дате? Да/Нет_
> 
>_> да_

Программа сортирует транзакции по дате
>_Отсортировать по возрастанию или по убыванию?_ 
>
>_по возрастанию/по убыванию_

Клиенту предлагается отсортировать по рублевым транзакциям
>_Выводить только рублевые транзакции? Да/Нет_
>
>_> да_

Далее программа уточняет отсортировать ли по определенному слову
>_Отфильтровать список транзакций по определенному слову 
в описании? Да/Нет_
>
>_> да/нет_

Программа уточняет, по какому слову клиент хочет отсортировать:
>_Введите слово для поиска:_
> 
> _> Перевод_
> 
Клиент вводит необходимое ему слово и программа начинает подготавливать список транзакций
>_Распечатываю итоговый список транзакций..._


>_Всего банковских операций в выборке: 8_

Итогом программы будет
```
09.06.2022 Перевод с карты на карту
Visa 4756 82** **** 5661 -> Discover 6499 54** **** 6238
Сумма: 30387 RUB

11.04.2022 Перевод организации
Mastercard 5612 50** **** 7911 -> Счет **0384
Сумма: 11280 RUB

08.02.2022 Перевод с карты на карту
Discover 6348 32** **** 5461 -> Mastercard 7325 94** **** 4710
Сумма: 15911 RUB

20.06.2021 Перевод с карты на карту
American Express 2932 52** **** 4117 -> American Express 3633 64** **** 8444
Сумма: 19321 RUB

19.02.2021 Перевод с карты на карту
Mastercard 9775 49** **** 9116 -> Discover 5944 50** **** 3459
Сумма: 12918 RUB

07.06.2020 Перевод с карты на карту
Visa 6644 69** **** 6766 -> Mastercard 8548 96** **** 6541
Сумма: 12234 RUB

25.02.2020 Перевод с карты на карту
Mastercard 4597 61** **** 2324 -> Visa 0264 84** **** 4307
Сумма: 17655 RUB

06.02.2020 Перевод с карты на карту
Mastercard 7673 26** **** 9337 -> Discover 6961 58** **** 5561
Сумма: 12480 RUB
```

### Модуль `masks`
* `get_mask_card_number`, которая принимает на вход номер карты и возвращает ее маску, в которой видны первые 6 цифр и последние 4 цифры, остальные символы отображаются звездочками, номер разбит по блокам по 4 цифры, разделенным пробелами. Пример работы функции:
  ```
  7000792289606361     # входной аргумент
  7000 79** **** 6361  # выход функции
  ```
* `get_mask_account`, которая принимает на вход номер счета и возвращает его маску, видны только последние 4 цифры номера, а перед ними — две звездочки. Пример работы функции:
  ```
  73654108430135874305  # входной аргумент
  **4305  # выход функции
  ```
  
### Модуль `widget` 
* `mask_account_card`, которая умеет обрабатывать информацию о картах, и счетах, используется маскировка описанная выше.
* `get_date`,  которая принимает на вход строку с датой в формате "2024-03-11T02:26:18.671407" и возвращает строку с датой в формате "ДД.ММ.ГГГГ" ("11.03.2024") Пример работы функции:
   ```
  2024-03-11T02:26:18.671407  # входной аргумент
  11.03.2024  # выход функции
  ``` 
### Модуль `processing`  
* `filter_by_state`, которая принимает список словарей и опционально значение для ключа `state` (по умолчанию 'EXECUTED').
  Функция возвращает новый список словарей, содержащий только те словари, у которых ключ `state` соответствует указанному значению.
* `search_banking_transactions_by_string` функция принимает список словарей с данными о банковских операциях и строку поиска,
    а возвращает список словарей, у которых в описании есть данная строка.
* `get_count_transactions_category` функция принимает список словарей с данными о банковских операциях и список категорий операций, а возвращает
    словарь, в котором ключи — это названия категорий, а значения — это количество операций в каждой категории.

* `sort_by_date`, которая принимает список словарей и необязательный параметр, задающий порядок сортировки (по умолчанию — убывание). Возвращает новый список, отсортированный по дате (`date`).

### Модуль `generators`
* `filter_by_currency` фильтрует транзакции по заданной валюте (например USD).
* `transaction_description(transactions)` генератор, возвращающий описания транзакций.
* `card_number_generator(start, stop)` генератор номеров банковских карт в заданном диапазоне в формате "XXXX XXXX XXXX XXXX", где Х - цифры карты.

### Модуль `decorators`
* `log` добавлен декоратора который автоматически логирует начало и конец выполнения функции, а также ее результаты или возникшие ошибки.
* `my_function` параметр с которым работает декоратор `log` 

### Модуль `utils`
* `get_info_about_transactions` функция принимает на вход путь до JSON-файла и возвращает список словарей с данными о финансовых транзакциях.

### Модуль `external_api`
* `transaction_amount` функция ковертирует из USD в RUB используя файл JSON и возвращает ответ через API запрос

### Модуль `reader_csv_xlsx`
* `reader_transactions_csv` функция принимает на вход пусть до CSV-файла и выдает список словарей с транзакциями
* `reader_transactions_xlsx` функция принимает на вход пусть до Excel-файла и выдает список словарей с транзакциями.

## Тестирование программы

Для всех модулей добавлены тест-кейсы:
* В `test_masks` были добавлены тесты для функций `get_mask_card_number` - проверяет правильность маскировки номера карты, `get_mask_account` - проверяет правильность маскирования номера счета.
* В `test_widget` были добавлены тесты для функций `mask_account_card` - корректно распознает и применяет нужный тип маскировки в зависимости от типа входных данных (карта или счет). и `get_date` проверяет правильность преобразования даты.
* В `test_processing` были добавлены тесты для функций `filter_by_state` - проверяет фильтрацию списка словарей по заданному статусу `state`, `sort_by_date` проверят корректность сортировки по дате.
* В `test_generators` были добавлены тесты для функций `filter_by_currency` - проверят корректность фильтрации по заданной валюте, `transaction_descriptions` - проверят, что функция возвращает корректные описания для каждой транзакции, и `card_number_generator` проверяют, что генератор выдает правильные номера карт в заданном диапазоне. 
* В `test_decorators` были добавлены тесты, проверяющие функциональность декоратора.
* В `test_utils` были добавлены тесты проверяющие работоспособность чтения файла.
* В `test_external_api` были добавлены тесты для проверки конвертации. 

## Логирования функций

Было добавлено логирование фукнций в отдельный файл `mylog.txt` в котором прописывается время вызова, имя функции, передаваемые аргументы, результат выполнения и информация об ошибках.
Добавлено логирование модулей `utils` и `masks`